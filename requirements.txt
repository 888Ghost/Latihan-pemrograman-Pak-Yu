# Polymarket Weather Bot v13.0 — Runtime dependencies
# Bug#8 FIX: removed py-clob-client-v2 (does not exist on PyPI)
# Bot uses direct eth-account L1 signing — no CLOB SDK needed at runtime
numpy>=1.24.0
scipy>=1.10.0
requests>=2.28.0
python-dateutil>=2.8.0
eth-account>=0.9.0
